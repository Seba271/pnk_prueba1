<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.WebService" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Fondo general de la app -->
        <item name="android:windowBackground">@color/colorFondo</item>

        <!-- Colores primarios -->
        <item name="colorPrimary">@color/colorPrimario</item>
        <item name="colorPrimaryVariant">@color/colorPrimario</item>

        <!-- Texto sobre color primario (botones por ejemplo) -->
        <item name="colorOnPrimary">@color/colorBotonTexto</item>

        <!-- Color general del texto -->
        <item name="android:textColor">@color/colorTextoGeneral</item>
    </style>
</resources>
